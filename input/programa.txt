int x = 10;
int y = 20;

int sumar(int a, int b) {
    return a + b;
}

int multiplicar(int a, int b) {
    return a * b;
}

int z = sumar(x, y);
int w = multiplicar(z, 3);

if (x < y) {
    int x = 99;
    int resultado = sumar(x, w);
}